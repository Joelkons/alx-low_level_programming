#include "main.h"

/**
 * print_line - draws a straight line in the terminal
 * @n: number of lines to print
 */

void print_line(int n)
{
	int a;

	if (a > 0)
		for (a = 0; a < n; a++)
			_putchar('_');
	_putchar('\n');
}
